// 测试注释
/* casd


*/
void scanner (FILE * fp) {
    char ch;
    int i, c;
    ch = fgetc(fp);
    if (isalpha(ch)) {
        while(isalnum(ch)) {
            ch = fgetc(fp);
        }
    }
    else
}